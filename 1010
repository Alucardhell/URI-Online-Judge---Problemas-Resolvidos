#include <iostream>
#include <stdio.h>
using namespace std;

int main()
{
	int codp, nump, codp2, nump2;
	float valp, valp2, total;

	cin >> codp;
	cin >> nump;
	cin >> valp;
	cin >> codp2;
	cin >> nump2;
	cin >> valp2;

	total = (nump * valp) + (nump2 * valp2);

	printf("VALOR A PAGAR: R$ %.2f\n", nump * valp + nump2 * valp2);

	return 0;
}
