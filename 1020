#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n, a = 0, m = 0;

	cin >> n;

	while (n >= 365)
	{
		a++;
		n = n - 365;
	}
	while (n >= 30)
	{
		m++;
		n = n - 30;
	}

	printf ("%d ano(s)\n", a);
	printf ("%d mes(es)\n", m);
	printf ("%d dia(s)\n", n);

	return 0;


}
